# Stock Radar 运行依赖
# 仅使用 requests + pandas + flask + Python 标准库，不依赖 akshare 等未安装的三方包。
# 安装：D:\Python3.12.1\python.exe -m pip install -r requirements.txt
requests>=2.28.0
pandas>=1.5.0
Flask>=2.2.0
（内容由AI生成，仅供参考）
